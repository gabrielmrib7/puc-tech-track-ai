# Design

Build customer presentation services that always scope queries by authenticated customer identity. Transform domain statuses into a typed localized view model. Implement responsive mobile-first cards and timeline using the existing Technical Precision System, with accessible labels and no raw enum values. Add API, unit, integration, and mobile E2E coverage.
