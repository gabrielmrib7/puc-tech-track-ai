# Design

Implement dashboard metrics as an authorized application query with indexed aggregations and typed DTOs. Implement delivery as a transactional use case that validates READY_FOR_PICKUP, records pickup data and history, and relies on the domain terminal-state guard for all later mutations. Build the desktop dashboard and delivery confirmation UI after API tests.
